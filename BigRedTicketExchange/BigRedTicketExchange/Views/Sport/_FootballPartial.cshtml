@model BigRedTicketExchange.Models.Sport

<div class="body-content">
    <table class="table table-hover">
        @foreach (var game in Model.Games)
        {
            <tr>
                <td>
                    @game.Opponent
                </td>
                <td>
                    @game.Date
                </td>
            </tr>
        }
    </table>


</div>